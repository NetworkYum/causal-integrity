/-
  Causal Integrity Framework
  Lean 4 Lake library (S1 slice)

  Core idea: coherent causality preferred by action (foundation);
  Property = Captured Causality (proof instrument).

  Status (honest):
  - Lake package builds (`lake build`)
  - Core axioms 0–4, 6 encoded (incl. agent filter + liability)
  - Consent expanded as a definition; Capable partial; Coerced primitive
  - Non-trivial lemmas: theft/homestead chains, liability≠ownership,
    agent-filter consequences, consent hygiene, preference link
  - Axiom 5 (UPB / performative contradiction): NOT encoded (scoped out)
  - Forward simulation: NOT encoded (S2/S3)
  - Still a skeleton / hypothesis — not a complete verified ethics OS
-/

import CausalIntegrity.Axioms
import CausalIntegrity.Lemmas
