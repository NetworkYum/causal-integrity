name: Lean

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    name: lake build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install elan and build
        uses: leanprover/lean-action@v1
        with:
          build-args: ""
